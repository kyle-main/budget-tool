import { DateQuarterPipe } from './date-quarter.pipe';

describe('DateQuarterPipe', () => {
  it('create an instance', () => {
    const pipe = new DateQuarterPipe();
    expect(pipe).toBeTruthy();
  });
});
